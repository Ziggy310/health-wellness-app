# Health App Environment Variables Template
# Copy this file to .env and fill in your actual values

# Supabase Configuration (Optional - for database features)
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# App Configuration
VITE_APP_NAME=Health & Wellness App
VITE_APP_VERSION=1.0.0

# API Keys (if needed for external services)
# VITE_HUGGINGFACE_API_KEY=your_huggingface_key_here

# Analytics (Optional)
# VITE_GOOGLE_ANALYTICS_ID=your_ga_id_here

# Note: All environment variables used in Vite must be prefixed with VITE_
# These will be embedded in the client bundle, so don't include sensitive server-side secrets